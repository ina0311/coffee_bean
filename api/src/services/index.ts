import * as coffeeBeanService from "./coffeeBean.service"
import * as storeService from "./store.service"
import * as googleMap from "./googleMap"

export default {
  coffeeBeanService,
  storeService,
  googleMap,
}